// Dependencies
import {StyleSheet} from 'react-native';

// Themes
import {colorBackground} from '../../themes/colors';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: colorBackground
  }
});
