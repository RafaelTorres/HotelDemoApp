// Dependencies
import {StyleSheet} from 'react-native';

// Themes
// import {} from '../../themes/colors';

export default StyleSheet.create({
  centered: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0
  },
  image: {
  }
});
