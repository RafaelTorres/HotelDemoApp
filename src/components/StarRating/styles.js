// Dependencies
import {StyleSheet} from 'react-native';

// Themes
import {color1} from '../../themes/colors';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center'
  },
  stars: {
    fontSize: 18,
    marginRight: 5,
    color: color1
  }
});
