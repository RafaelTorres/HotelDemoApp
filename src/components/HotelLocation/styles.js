// Dependencies
import {StyleSheet} from 'react-native';

// Themes
import {color2} from '../../themes/colors';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 10
  },
  address: {
    fontSize: 16,
    marginLeft: 5,
    color: color2
  },
  buttonContainer: {
    marginTop: 10
  }
});
