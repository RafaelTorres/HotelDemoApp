// Dependencies
import {StyleSheet} from 'react-native';

// Themes
import {color2} from '../../themes/colors';

export default StyleSheet.create({
  message: {
    flex: 1,
    alignSelf: 'center',
    marginTop: 15,
    color: color2,
    fontSize: 18
  }
});
